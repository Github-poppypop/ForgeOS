# CEO runbook
Display only — NOT auto-executed.

1. OKRs must reference the `org` charter direction.
2. Cross-domain conflicts: cite charter, not preference.
3. Board report uses `gbrain think` for cited synthesis + gap analysis.
