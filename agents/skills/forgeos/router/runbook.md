# Router runbook
Post-scaffold display only — NOT auto-executed (supply-chain hardening).

1. The router reads `ORG.md §2/§4` to resolve ownership.
2. On first use, it seeds the routing table from `profiles/c-suite/`.
3. Keep triggers MECE — no two role skills share an identical trigger phrase.
4. Ambiguous intent → one clarifying question, else escalate to CEO.
