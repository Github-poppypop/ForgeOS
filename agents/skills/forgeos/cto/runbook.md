# CTO runbook
Display only — NOT auto-executed.

1. Every architecture change = a `decision` page (ADR). No silent changes.
2. Incident → open `incident`, link `escalated_to` if it crosses domains.
3. Deploy sign-off is co-owned with COO per ORG §3.6.
