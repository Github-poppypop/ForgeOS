# Board runbook
Display only — NOT auto-executed.

1. Board actions require quorum; record votes in the `org` timeline.
2. Never edit DNA/agent runtime directly — route to CEO.
3. All ratified actions become `decision` pages (audit trail).
