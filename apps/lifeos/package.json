{
  "name": "lifeos",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "LifeOS engine runtime stubs (ForgeOS flagship app).",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
