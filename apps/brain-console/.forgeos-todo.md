# ForgeOS Brain Console — durable task ledger

## Completed
- [x] All phases 1-11 implemented
- [x] 74/74 unit tests passing on VPS
- [x] Tooltips expanded to all UI elements
- [x] Keyboard shortcuts cheatsheet (`?` key)
- [x] Empty state illustrations (`emptyState()`)
- [x] Print styles (`@media print`)
- [x] Column visibility toggles (`columnToggle()`)
- [x] Bulk actions for vault/audit (`bulkActions()`)
- [x] Confirmation modals (`confirmAction()`)
- [x] Drag-and-drop kanban (`initKanban()`)
- [x] Live search/filter (`initLiveSearch()`, 200ms debounce)
- [x] Mobile responsive CSS (768px/480px breakpoints)
- [x] Webhook management UI (`renderWebhooks()`)
- [x] Virtual scrolling helper (`initVirtualScroll`)
- [x] Sentry error tracking (SDK + `initSentry()`)
- [x] Plugin sandboxing (`PLUGIN_TIMEOUT=5000ms`, `Promise.race`)
- [x] Batch capture endpoint (`POST /api/capture/batch`)
- [x] Export/import endpoints (`GET /api/export/:slug`, `POST /api/import`)
- [x] Prometheus metrics endpoint (`GET /api/metrics/prometheus`)
- [x] Push notifications (`requestNotificationPermission`, `showNotification`)
- [x] Offline capture queue (`queueCapture`, `flushOfflineQueue`)
- [x] Task Scheduler wrapper script created at `scripts/start-server.bat`
- [x] VPS hermes provider configured and working (`stepfun/step-3.7-flash:free`)
- [x] tmux agent pipeline verified on VPS
- [x] Autonomous improvement cron installed (`/opt/forgeos/scripts/improve.sh`)
- [x] GitHub Actions CI workflows (lint, unit, e2e)

## Manual actions required
- [ ] **Task Scheduler**: update `ForgeOSBrainConsole` action to `scripts\start-server.bat` (see `docs/task-scheduler-setup.md`)
- [ ] **Self-hosted runner**: run `scripts/setup-runner.ps1 -Token <token>` on Windows as Administrator (see `docs/self-hosted-runner.md`)

## In progress
- [ ] **E2E testing**: Playwright tests fixed, awaiting Windows runner activation
- [ ] **Plugin hot-reload UI**: endpoint exists, button needed in plugins panel
- [ ] **Webhook dead-letter queue UI**: endpoint exists, admin panel needed

## Backlog (priority order)

### UI/UX polish
- [ ] **Model fallback chains**: wire `MODEL_FALLBACKS` into actual model call path
- [ ] **Font size/contrast controls**: expose in `renderSettings` with live preview
- [ ] **Theme marketplace**: visual picker for 6+ themes
- [ ] **Confirmation modals everywhere**: audit all destructive actions
- [ ] **Keyboard shortcuts on all panels**: wire `?` overlay globally
- [ ] **Column visibility on all tables**: vault, audit, missions
- [ ] **Tooltips on all form fields**: complete coverage audit
- [ ] **Lazy panel loading**: `loadPanel()` for all 25 panels
- [ ] **Command palette**: `cmd+k` across all panels
- [ ] **Onboarding tour**: step-by-step for new users
- [ ] **Accessibility audit**: ARIA labels, focus management, screen reader support
- [ ] **Loading skeletons**: all async data loads
- [ ] **Toast notifications**: global toast system for all actions
- [ ] **Error boundaries**: graceful error states in all panels
- [ ] **Dark/light theme toggle**: per-user preference persistence

### Backend/API
- [ ] **OpenTelemetry tracing**: trace IDs frontend → API → gbrain
- [ ] **API contract tests (Pact)**: frontend/backend contract safety
- [ ] **GraphQL endpoint**: alternative query API
- [ ] **WebSocket real-time updates**: live brain sync
- [ ] **Request deduplication**: cache duplicate API calls
- [ ] **Health check timeouts**: configurable per-dependency
- [ ] **Agent memory persistence**: context across sessions
- [ ] **Mission scheduling**: cron-like timed captures
- [ ] **Multi-agent collaboration**: shared workspaces
- [ ] **Plugin marketplace**: discover/install from registry
- [ ] **Audit log export**: CSV/JSON/SQL dump
- [ ] **Rate limit dashboard**: per-user/IP metrics

### Testing/CI
- [ ] **Dependency scanning (Snyk)**: complement `bun audit`
- [ ] **Semantic release**: auto-version from commits
- [ ] **Conventional commits enforcement**: `commitlint` in pre-commit
- [ ] **Visual regression tests**: Percy/Chromatic for panels
- [ ] **Load testing**: k6/Artillery for `/api/*`
- [ ] **Fuzz testing**: capture slug validation
- [ ] **E2E test coverage**: Playwright for all 25 panels
- [ ] **Test fixtures**: seeded gbrain data for CI
- [ ] **Coverage reporting**: codecov integration

### Observability/security
- [ ] **OpenTelemetry tracing**: distributed traces
- [ ] **Structured logging**: JSON logs to file
- [ ] **Health dashboard**: `/api/health/detailed` UI
- [ ] **Sentry alerting**: Slack/email on errors
- [ ] **Security headers audit**: CSP, HSTS, X-Frame-Options
- [ ] **Dependency scanning**: Snyk + `bun audit`
- [ ] **Secret rotation**: JWT_SECRET, CONSOLE_TOKEN
- [ ] **Audit trail**: immutable action log

### Documentation
- [ ] **API reference**: OpenAPI docs
- [ ] **Developer onboarding**: setup guide
- [ ] **Architecture diagram**: system design doc
- [ ] **Contributing guide**: PR workflow
- [ ] **Changelog**: keep-a-changelog format
- [ ] **Video walkthrough**: 5-min demo

### AI/agent
- [ ] **Agent memory**: persistent context
- [ ] **Multi-agent**: parallel task dispatch
- [ ] **Tool use**: function calling
- [ ] **RAG**: retrieval-augmented generation
- [ ] **Fine-tuning**: custom models
- [ ] **Agent marketplace**: discover agents

### Deployment/infra
- [ ] **Docker container**: multi-stage build
- [ ] **Kubernetes manifests**: deploy to k8s
- [ ] **Terraform**: infra as code
- [ ] **Blue/green deploy**: zero-downtime
- [ ] **CDN**: static asset caching
- [ ] **WAF**: Cloudflare/AWS WAF

## Verified
- [x] `hermes -z` works on VPS with Nous provider
- [x] Unit tests: 74/74 passing on VPS (`bun test tests/unit/*.spec.ts`)
- [x] Server boots on VPS: `PORT=7777 bun run server.ts`
- [x] All `/api/*` routes return expected status
- [x] `node --check src/app.js` passes
- [x] `src/app.ts` synced from `src/app.js`
- [x] No `bun build` dependency introduced
- [x] VPS git at `origin/master` (`5ae27a1`)
