# ForgeOS Brain Console — durable task ledger
# Survives session close. Orchestrator cron reads/writes this each cycle.

## Phase 1-11 — complete (see git history)
- All Phase 1-10 features committed
- Phase 11 UI: wizard, projects, settings, workflows, marketplace, plugins

## Productionization — remaining
- [ ] 40. Task Scheduler wrapper script
- [ ] 41. Self-hosted runner setup (user action required)
- [ ] 42. tmux agent e2e pipeline
- [ ] 44. Verify e2e on self-hosted runner

## Known issues (non-blocking)
- [ ] capture.spec.ts requires isolated port (skipped when :7777 in use)
- [ ] VPS git submodule apps/poolleague/.git is broken (use git clone --filter=blob:none)
