# ForgeOS Brain Console — durable task ledger

_Last refreshed: 2026-08-19 by forgeos-driver. Source-verified against
`apps/brain-console/src/client/src/App.tsx`, `server.ts`, `src/server/runtime.ts`,
`src/server/features/*`, `src/client/src/features/*`. The app is TypeScript
(React + Express + Vite) served from `dist/` on :7777. Features are added via a
conflict-free discovery layer: server `src/server/features/feat-*.ts` (loaded by
`features/loader.ts` into the runtime router) and client `src/client/src/features/feat-*.tsx`
(discovered by `features/registry.ts` via Vite `import.meta.glob`, mounted by
`features/dispatch.tsx` in App.tsx). New features NEVER edit App.tsx/runtime.ts/server.ts._

## Verified completed (real, in source — do NOT re-implement)
- [x] Build green: `npm run build` (client) exits 0; `npm test` 21/21 pass.
- [x] Server hardening: `applySecurityHeaders` + `structuredLog` (daily JSON) + env-gated `alertError`.
- [x] In-memory rate limiting on public mutation endpoints + `/api/rate-limit/status` snapshot.
- [x] CSV export on Ledger, Vault, Missions, Audit.
- [x] Live rate-limit status card on Monitoring panel.
- [x] Command palette (CMD+K), shortcuts overlay, theme/contrast swatches, skip-link, ARIA roles,
      `DebugErrorBoundary`, `Skeleton`, `OnboardingTour`, `EmptyState`.
- [x] Playwright e2e + performance-budget scaffold; a11y pass.
- [x] Self-improve agent runtime status chip + `/api/self-improve/learning-loop`, `/api/feedback`, `/api/telemetry`.
- [x] OpenAPI spec generated (`openapi.json`, 70+ paths).
- [x] **SSE live brain sync** — `src/server/sse.ts` + `features/feat-sse.ts` registers `GET /api/brain/stream`;
      client `features/feat-sse.tsx` (`/feature/sse`) opens an EventSource. VERIFIED live (200, emits frames).
- [x] **Audit log export to SQL/JSON** — `src/server/auditExport.ts` + `features/feat-auditexport.ts`
      adds `GET /api/audit/export?format=sql|json`. VERIFIED live (200 both).
- [x] **Rate-limit dashboard** — `src/client/src/RateLimitDashboard.tsx` mounted via `features/feat-ratelimit.tsx`
      (`/feature/ratelimit-dash`), polls `/api/rate-limit/status`. VERIFIED in bundle.
- [x] **CI scaffold** — `.github/workflows/ci.yml` runs build:client + npm test + `npm audit` + snyk (continue-on-error).
- [x] **Semantic release + commitlint** — `.releaserc.json`, `commitlint.config.js`.
- [x] **Docker multi-stage build** — `apps/brain-console/Dockerfile`.
- [x] **docker-compose** — `apps/brain-console/docker-compose.yml`.
- [x] **K8s manifests** — `infrastructure/k8s/forgeos.yaml`.
- [x] **Terraform VPS provisioning** — `infrastructure/terraform/main.tf` (+ lock).
- [x] **Load-testing scaffold** — `infrastructure/loadtest/forgeos.k6.js` (Artillery yml also in repo).
- [x] **API reference doc** — `docs/developers/api-reference.md`.
- [x] **Developer onboarding guide** — `docs/developers/onboarding-guide.md`.

## Genuine remaining backlog (real gaps — pick from here)
### Real-time / observability
- [ ] OpenTelemetry trace IDs frontend → API → gbrain (WAVE 2 in flight).
- [ ] Sentry/alerting *wiring*: `alertError` implemented; no `SENTRY_DSN`/`ALERT_WEBHOOK_URL` in VPS env.
      Add config-status + test endpoint (WAVE 2 in flight).
### Backend/API
- [ ] Agent memory persistence across sessions (external blocker: needs authenticated agent CLI).
- [ ] Mission scheduling (cron-like timed captures).
- [ ] Multi-agent collaboration / shared workspaces.
- [ ] Plugin marketplace registry (discover/install from remote).
- [ ] Rate-limit *dashboard* per-route breakdown (snapshot exists; only a summary card + total shown).
### Testing/CI
- [ ] Dependency scanning (Snyk) ACTUALLY wired (CI step present but `snyk` not installed in image).
- [ ] Visual regression (Percy/Chromatic) — needs token; scaffold only.
- [ ] Coverage reporting (codecov).
- [ ] Webhook management UI (WAVE 2 in flight).
- [ ] Drag-and-drop kanban (WAVE 2 in flight).
- [ ] Bulk actions vault/audit (WAVE 2 in flight).
- [ ] Confirmation modals for destructive actions (WAVE 2 in flight).
- [ ] PWA offline mode (WAVE 2 in flight).
- [ ] GraphQL endpoint + explorer (WAVE 2 in flight).
### Docs
- [ ] Architecture diagram + changelog (mermaid in docs).
- [ ] Published API reference served from `openapi.json` at a live route (doc file exists; no `/api/docs` route yet).

## Known external blockers (not resolvable by code alone)
- Self-improve agent loop (`POST /api/agent/self-improve/run`) shells out to an agent CLI not
  authenticated on the VPS — needs `FORGEOS_AGENT` set to a usable agent or API key.
- Alerting is a silent no-op until `SENTRY_DSN` or `ALERT_WEBHOOK_URL` env is set.
- `apps/poolleague` shows as a dirty/untracked entry in `git status` but is not present on disk;
  left uncommitted intentionally (unrelated local artifact).

## Wave 1 driver log (2026-08-19, forgeos-driver)
- 12 parallel `delegate_task` subagents shipped (one per backlog item, each in an isolated git
  worktree, build-verified, merged to master, branches pruned): sse, ratelimit-dash, audit-export,
  dockerfile, docker-compose, k8s, terraform, ci, release, k6, api-doc, onboarding.
- Direct server.ts wiring also added (commit 705ef42): `GET /api/stream` SSE hub + `GET /api/audit/export`
  (in addition to the conflict-free features/ layer at `/api/brain/stream`). Both VERIFIED live (200).
- Build green (tsc+vite, 37 modules), `npm test` 21/21, PM2 `forgeos` online on :7777, health 200.
- COORDINATION NOTE: a second autonomous process (RealtyIQ Autopilot, commit 67d2c73) independently
  wired the same wave1 files via the conflict-free features/ layer and refreshed this ledger. Both
  approaches coexist (distinct route paths); no merge conflicts observed.
