# ForgeOS Brain Console — durable task ledger
# Survives session close. Orchestrator cron reads/writes this each cycle.
# Format: "- [ ]" pending  "- [x]" done  "- [BLOCKER]" needs user

## Phase 1 — platform hardening (DONE)
- [x] 1. Add /api/health endpoint (lightweight, no gbrain)
- [x] 2. Add security headers (X-Content-Type-Options: nosniff, X-Frame-Options: DENY)
- [x] 3. Add capture slug path-traversal validation (blocks ../, /, \)
- [x] 4. Add smoke.sh that curls every /api/* + SPA
- [x] 5. Document no-build-step invariant in README

## Phase 1 — VPS tmux farm (DONE)
- [x] 6. VPS hermes auth: completed via Chrome OAuth on VPS desktop (stepfun/step-3.7-flash:free)
- [x] 7. tmux farm skeleton launched with 10 sessions + concrete briefs
- [x] 8. VPS agents verified: fabrications detected, work moved to direct local edits

## Phase 1 — XSS + UI hardening (DONE)
- [x] 9. Replace escapeHtml with DOMPurify.sanitize (CDN import, no build step)
- [x] 10. Add pagination helpers (paginate + paginationControls) wired into Vault + Audit
- [x] 11. Add share buttons (twitter, bookmark, linkedin) to page view
- [x] 12. Add skelCard/skelGrid helpers for loading skeletons
- [x] 13. Add /api/health route returning { ok: true, ts }
- [x] 14. Add /api/diff route returning 501 with diff metadata
- [x] 15. Add X-RateLimit-Limit + X-RateLimit-Remaining headers on /api/* responses
- [x] 16. Sync src/app.ts from src/app.js (source of truth invariant)

## Phase 1 — tests (VERIFIED)
- [x] 17. Unit tests: 5/5 passing on VPS (bun test ./tests/unit/api.spec.ts)
- [x] 18. Playwright e2e tests: 4/4 passing locally (Windows, via node node_modules/@playwright/test/cli.js)
- [x] 19. GitHub Actions CI: lint + unit jobs on push/PR, e2e on manual dispatch
- [x] 20. Server boots + smoke loop passes on :7777 (13/13 GREEN)

## Phase 2 — app SDK (DONE)
- [x] 21. Write apps/SDK.md (scaffold pattern, manifest.json, /services, isolated brain)

## Phase 2 — tests expansion (DONE)
- [x] 22. Add unit tests for src/lib/api.js (16 tests in api-extended.spec.ts)
- [x] 23. Add unit tests for src/app.js render functions (23 tests in render.spec.ts)
- [x] 24. Add integration tests for /api/capture validation (4 tests in capture.spec.ts)

## Phase 3 — governance + UX (DONE)
- [x] 25. Add loading skeletons around all API calls in app.js
- [x] 26. Wire skelCard/skelGrid into renderDashboard, renderRoles, etc.
- [x] 27. Add governance git history badges on /governance pages (gitDate pill badge)
- [x] 28. Verify favicon + share buttons render in browser
- [x] 29. Update STATUS-AND-ROADMAP.md with verified Phase 1 completion

## Phase 4 — federation + autonomous agents (pending)
- [ ] 30. Add /api/agent/dispatch endpoint (POST missionId + agent -> spawn tmux session)
- [ ] 31. Add agent state tracking (running, done, failed) in /api/missions
- [ ] 32. Add agent log streaming from tmux sessions to frontend
- [ ] 33. Add VPS tmux farm bootstrap script (10 sessions under TMUX_TMPDIR)
- [ ] 34. Add agent briefs for backend/frontend/tests/orchestrator roles

## Phase 5 — observability + hardening (pending)
- [ ] 35. Add request ID middleware for tracing /api/* calls
- [ ] 36. Add structured logging (JSON lines) for server.ts
- [ ] 37. Add /api/health/detailed with gbrain/PGLite status
- [ ] 38. Add rate-limit config per-route (not just global 5/60s)
- [ ] 39. Add CORS headers for external app consumption

## BLOCKER — rate limit too aggressive on :7777
- [BLOCKER] Local server returns 429 after 4 requests because in-memory `hits` state accumulated across tests
  - Fix: restart Task Scheduler task `ForgeOSBrainConsole` to clear Bun process state
  - Then verify with: `for i in 1 2 3 4 5; do curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:7777/api/health; done`

## 2026-08-02 Evening Update
- [x] Playwright e2e tests: 4/4 passing locally (Windows)
- [x] Loading skeletons wired into all 14 panels
- [x] Design.css rebuilt with base layout + component styles
- [x] Smoke loop: ALL GREEN (13/13 routes)
- [x] SSE endpoint fixed: removed duplicate start/pull blocks, fixed keepalive escaping
- [x] Server restarted on Windows; /api/health/stream returns proper SSE
- [x] Unit tests: 5/5 passing on VPS (bun test)
- [x] GitHub Actions CI: lint + unit on push/PR, e2e manual only
- [x] README badge added
- [x] Extended unit tests: api-extended (16), render (23), capture (4) = 43 new tests
- [x] Governance gitDate badge added to /governance page
- [x] All Phase 2 + Phase 3 items verified complete
