# ForgeOS Brain Console — durable task ledger

_Last refreshed: 2026-08-19 by forgeos-driver. Source-verified against
`apps/brain-console/src/client/src/App.tsx`, `server.ts`, `src/server/runtime.ts`,
`src/server/features/*`, `src/client/src/features/*`, and live `/api/*` route probes.
The app is TypeScript (React + Express + Vite) served from `dist/` on :7777. Features are
added via a conflict-free discovery layer: server `src/server/features/feat-*.ts` (loaded by
`features/loader.ts` into the runtime router) and client `src/client/src/features/feat-*.tsx`
(discovered by `features/registry.ts` via Vite `import.meta.glob`, mounted by
`features/dispatch.tsx` in App.tsx). New features NEVER edit App.tsx/runtime.ts/server.ts._

## Verified completed (real, in source — do NOT re-implement)
- [x] Build green: `npm run build` (client) exits 0; `npm test` 64/64 pass.
- [x] Server hardening: `applySecurityHeaders` + `structuredLog` (daily JSON) + env-gated `alertError`.
- [x] In-memory rate limiting on public mutation endpoints + `/api/rate-limit/status` snapshot.
- [x] CSV export on Ledger, Vault, Missions, Audit.
- [x] Live rate-limit status card on Monitoring panel.
- [x] Command palette (CMD+K), shortcuts overlay, theme/contrast swatches, skip-link, ARIA roles,
      `DebugErrorBoundary`, `Skeleton`, `OnboardingTour`, `EmptyState`.
- [x] Playwright e2e + performance-budget scaffold; a11y pass.
- [x] Self-improve agent runtime status chip + `/api/self-improve/learning-loop`, `/api/feedback`, `/api/telemetry`.
- [x] OpenAPI spec generated (`openapi.json`, 70+ paths) + live `/api/docs` viewer + `/api/openapi.json` raw.
- [x] **SSE live brain sync** — `feat-sse.ts` registers `GET /api/brain/stream`; `feat-sse.tsx` (`/feature/sse`) opens an EventSource. VERIFIED live 200.
- [x] **Audit log export to SQL/JSON** — `feat-auditexport.ts` adds `GET /api/audit/export?format=sql|json`. VERIFIED live 200.
- [x] **Rate-limit dashboard (per-route breakdown)** — `feat-ratelimit.tsx` (`/feature/ratelimit-dash`) polls `/api/rate-limit/routes`. VERIFIED live 200.
- [x] **CI scaffold** — `.github/workflows/ci.yml` (build + test + `npm audit` + snyk continue-on-error).
- [x] **Semantic release + commitlint** — `.releaserc.json`, `commitlint.config.js`.
- [x] **Docker multi-stage build** — `apps/brain-console/Dockerfile`; **docker-compose**; **K8s** `infrastructure/k8s/forgeos.yaml`; **Terraform** `infrastructure/terraform/main.tf`.
- [x] **Load-testing scaffold** — `infrastructure/loadtest/forgeos.k6.js` (Artillery yml also in repo).
- [x] **API reference doc** — `docs/developers/api-reference.md`; **Developer onboarding guide** — `docs/developers/onboarding-guide.md`.
- [x] **Webhook management UI** — `feat-webhooks.ts` (`GET/POST /api/webhooks`, `POST /api/webhooks/:id/test`, `DELETE /api/webhooks/:id`) + `feat-webhooks.tsx` (`/webhooks`). VERIFIED live 200.
- [x] **OpenTelemetry trace-ID propagation** — `feat-otel.ts` (router middleware sets `x-trace-id`; `GET /api/otel`) + `feat-otel.tsx` (`/feature/otel`). VERIFIED live 200.
- [x] **Alerting config-status + self-test** — `feat-alert-status.ts` (`GET /api/alerting/status`, `POST /api/alerting/test`) + `feat-alert-status.tsx` (`/feature/alert-status`). VERIFIED live 200.
- [x] **Mission scheduler** — `feat-missionsched.ts` (`GET/POST /api/missions/schedule`, `GET /api/missions/schedule/run/:id`) + `feat-missionsched.tsx` (`/feature/mission-scheduler`). VERIFIED live 200.
- [x] **Plugin marketplace registry** — `feat-marketplace-reg.ts` (`GET /api/marketplace/registry`, `POST /api/marketplace/registry/install`) + `feat-marketplace-reg.tsx` (`/feature/marketplace-registry`). VERIFIED live 200.
- [x] **Drag-and-drop kanban** — `feat-kanban.ts` (`GET/POST /api/kanban`) + `feat-kanban.tsx` (`/feature/kanban`). VERIFIED live 200.
- [x] **Bulk actions (vault)** — `feat-bulkvault.tsx` (`/feature/bulk-actions`) with `POST /api/vault/bulk-delete` + `POST /api/vault/bulk-export`. VERIFIED live 200.
- [x] **Confirmation modals** — `feat-confirm.tsx` (`/feature/confirm-modal`) + `feat-confirm.ts` (`POST /api/demo/reset-config`, `DELETE /api/demo/audit`). VERIFIED live 200.
- [x] **PWA offline mode** — `public/manifest.webmanifest` served at `GET /manifest.webmanifest` (200) + `public/sw.js` (200) + `feat-pwa.tsx` (`/feature/pwa`). VERIFIED live 200.
- [x] **GraphQL endpoint + explorer** — `feat-graphql.ts` (`POST /api/graphql`) + `feat-graphql.tsx` (`/feature/graphql`). VERIFIED live 200.
- [x] **Architecture diagram + changelog** — `docs/ARCHITECTURE.md` (mermaid) + `CHANGELOG.md`; `feat-changelog.tsx` (`/feature/changelog`) Whats-New panel. VERIFIED live 200.
- [x] **Cross-platform killPort** — `server.ts` free-port via `fuser`/`ss` on Linux (no crash-loop on `pm2 restart`).
- [x] **Route-contract test suite** — real `node:test` suite (`tests 64 / pass 64`) replacing orphaned bun specs.

## Genuine remaining backlog (real gaps — pick from here; Wave 3 candidates)
### Backend/API
- [ ] Agent memory persistence across sessions (external blocker: needs authenticated agent CLI).
- [ ] Multi-agent collaboration / shared workspaces.
- [x] Webhook *delivery engine* — implemented (feat-webhooks.ts: EventEmitter bus + `publishWebhookEvent` + `POST /api/webhooks/publish` + `/api/webhooks/echo`); verified live 200 + delivery receipts on published events.
- [x] Mission scheduler *execution* — implemented (feat-missionsched-exec.ts); source-verified.
- [x] GraphQL depth/complexity limiter — already implemented (feat-graphql.ts: MAX_DEPTH=8, MAX_COMPLEXITY=200 guards); ledger was stale.
### Testing/CI
- [ ] Dependency scanning (Snyk) ACTUALLY wired (CI step present but `snyk` not installed in image).
- [ ] Visual regression (Percy/Chromatic) — needs token; scaffold only.
- [ ] Coverage reporting (codecov).
- [ ] Playwright tests actually exercised in CI (scaffold present, not run).
### Observability/Security
- [x] Rate-limit *enforcement* telemetry — implemented (feat-ratelimit-telemetry.ts); source-verified.
- [ ] Structured audit log to a real store (currently in-memory/file; no rotation policy beyond daily file).
- [ ] CSP report-only -> enforce (currently likely report-uri absent).
### Docs
- [ ] SECURITY-GAPS.md refresh (currently claims partial CSP while server sets only nosniff — stale).
- [ ] Per-feature README in `features/` (no docs per new feature module).

## Known external blockers (not resolvable by code alone)
- Self-improve agent loop (`POST /api/agent/self-improve/run`) shells out to an agent CLI not
  authenticated on the VPS — needs `FORGEOS_AGENT` set to a usable agent or API key.
- Alerting is a silent no-op until `SENTRY_DSN` or `ALERT_WEBHOOK_URL` env is set.
- `apps/poolleague` shows as a dirty/untracked entry in `git status` but is not present on disk;
  left uncommitted intentionally (unrelated local artifact).

## Wave 1 driver log (2026-08-19)
- 12 conflict-free features shipped via parallel `delegate_task` subagents (each in an isolated
  git worktree, build-verified, merged to master): sse, ratelimit-dash, audit-export, dockerfile,
  docker-compose, k8s, terraform, ci, release, k6, api-doc, onboarding.

## Wave 2 driver log (2026-08-19)
- 12 features shipped (webhooks, otel, alerting, mission-scheduler, marketplace-registry, kanban,
  bulk-actions, confirm-modal, pwa, graphql, rate-limit-per-route, architecture+changelog) + PWA
  manifest gap closed post-merge. Build green; `npm test` 64/64; PM2 restarted; all `/api/*` + `/feature/*`
  routes verified 200. Ledger refreshed to true state (prior "WAVE 2 in flight" notes were stale).


## Wave 3 driver log (2026-08-19)
- CI was effectively dead: `ci.yml` `unit` job ran `node --test __tests__` (only hit a
  failing pseudo-test; never reached the real 69-test suite in `src/server/__tests__/` +
  `tests/`), and its `lint` job ran `node --check server.ts` (only passes on Node >=22
  native-TS; CI used Node 20 -> would fail). Separate `lint.yml` ran `eslint src/**/*.js`
  (no .js present, no eslintrc) + `prettier --check` (prettier not a dependency) -- pure
  no-ops that masked failures.
- Rewrote `.github/workflows/ci.yml` into a working pipeline:
  - `build`: Node 22, `npm install` + `npm run build:client` (real tsc type-check + vite bundle).
  - `test`: Node 22, `npx tsx --test --experimental-test-coverage` -> lcov; Codecov upload
    gated by `CODECOV_TOKEN`. Verified locally on VPS: 69/69 pass, ~51KB lcov generated.
  - `security`: Snyk `snyk test` gated by `SNYK_TOKEN` (was only a scaffold mention).
  - `e2e`: now starts the console server before Playwright so the suite is actually exercisable.
- Deleted broken `.github/workflows/lint.yml` (no eslint/prettier config; its checks were
  no-ops masking failures).
- Ledger correction: tests are **69/69** (prior "64/64" was stale after feature waves).
- Build green; all 21 live `/api/*` routes verified 200 on VPS.
