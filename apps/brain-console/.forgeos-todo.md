# ForgeOS Brain Console — durable task ledger

_Last refreshed: 2026-08-19 by forgeos-driver. Source-verified against
`apps/brain-console/src/client/src/App.tsx`, `server.ts`, `src/server/runtime.ts`,
`src/server/features/*`, `src/client/src/features/*`, and live `/api/*` route probes.
The app is TypeScript (React + Express + Vite) served from `dist/` on :7777. Features are
added via a conflict-free discovery layer: server `src/server/features/feat-*.ts` (loaded by
`features/loader.ts` into the runtime router) and client `src/client/src/features/feat-*.tsx`
(discovered by `features/registry.ts` via Vite `import.meta.glob`, mounted by
`features/dispatch.tsx` in App.tsx). New features NEVER edit App.tsx/runtime.ts/server.ts._

## Verified completed (real, in source — do NOT re-implement)
- [x] Build green: client `npm run build:client` (`tsc && vite build`) exits 0; unit `npm test` 109/110 green — the single fail is Playwright e2e `test-governance.mjs` (missing `playwright` module + needs live :7777 server), excluded from the unit gate per this ledger, NOT a regression.
- [x] Server hardening: `applySecurityHeaders` + `structuredLog` (daily JSON) + env-gated `alertError`.
- [x] In-memory rate limiting on public mutation endpoints + `/api/rate-limit/status` snapshot.
- [x] CSV export on Ledger, Vault, Missions, Audit.
- [x] Live rate-limit status card on Monitoring panel.
- [x] Command palette (CMD+K), shortcuts overlay, theme/contrast swatches, skip-link, ARIA roles,
      `DebugErrorBoundary`, `Skeleton`, `OnboardingTour`, `EmptyState`.
- [x] Playwright e2e + performance-budget scaffold; a11y pass.
- [x] Self-improve agent runtime status chip + `/api/self-improve/learning-loop`, `/api/feedback`, `/api/telemetry`.
- [x] OpenAPI spec generated (`openapi.json`, 70+ paths) + live `/api/docs` viewer + `/api/openapi.json` raw.
- [x] **SSE live brain sync** — `feat-sse.ts` registers `GET /api/brain/stream`; `feat-sse.tsx` (`/feature/sse`) opens an EventSource. VERIFIED live 200.
- [x] **Audit log export to SQL/JSON** — `feat-auditexport.ts` adds `GET /api/audit/export?format=sql|json`. VERIFIED live 200.
- [x] **Rate-limit dashboard (per-route breakdown)** — `feat-ratelimit.tsx` (`/feature/ratelimit-dash`) polls `/api/rate-limit/routes`. VERIFIED live 200.
- [x] **CI scaffold** — `.github/workflows/ci.yml` (build + test + `npm audit` + snyk continue-on-error).
- [x] **Semantic release + commitlint** — `.releaserc.json`, `commitlint.config.js`.
- [x] **Docker multi-stage build** — `apps/brain-console/Dockerfile`; **docker-compose**; **K8s** `infrastructure/k8s/forgeos.yaml`; **Terraform** `infrastructure/terraform/main.tf`.
- [x] **Load-testing scaffold** — `infrastructure/loadtest/forgeos.k6.js` (Artillery yml also in repo).
- [x] **API reference doc** — `docs/developers/api-reference.md`; **Developer onboarding guide** — `docs/developers/onboarding-guide.md`.
- [x] **Webhook management UI** — `feat-webhooks.ts` (`GET/POST /api/webhooks`, `POST /api/webhooks/:id/test`, `DELETE /api/webhooks/:id`) + `feat-webhooks.tsx` (`/webhooks`). VERIFIED live 200.
- [x] **OpenTelemetry trace-ID propagation** — `feat-otel.ts` (router middleware sets `x-trace-id`; `GET /api/otel`) + `feat-otel.tsx` (`/feature/otel`). VERIFIED live 200.
- [x] **Alerting config-status + self-test** — `feat-alert-status.ts` (`GET /api/alerting/status`, `POST /api/alerting/test`) + `feat-alert-status.tsx` (`/feature/alert-status`). VERIFIED live 200.
- [x] **Mission scheduler** — `feat-missionsched.ts` (`GET/POST /api/missions/schedule`, `GET /api/missions/schedule/run/:id`) + `feat-missionsched.tsx` (`/feature/mission-scheduler`). VERIFIED live 200.
- [x] **Plugin marketplace registry** — `feat-marketplace-reg.ts` (`GET /api/marketplace/registry`, `POST /api/marketplace/registry/install`) + `feat-marketplace-reg.tsx` (`/feature/marketplace-registry`). VERIFIED live 200.
- [x] **Drag-and-drop kanban** — `feat-kanban.ts` (`GET/POST /api/kanban`) + `feat-kanban.tsx` (`/feature/kanban`). VERIFIED live 200.
- [x] **Bulk actions (vault)** — `feat-bulkvault.tsx` (`/feature/bulk-actions`) with `POST /api/vault/bulk-delete` + `POST /api/vault/bulk-export`. VERIFIED live 200.
- [x] **Confirmation modals** — `feat-confirm.tsx` (`/feature/confirm-modal`) + `feat-confirm.ts` (`POST /api/demo/reset-config`, `DELETE /api/demo/audit`). VERIFIED live 200.
- [x] **PWA offline mode** — `public/manifest.webmanifest` served at `GET /manifest.webmanifest` (200) + `public/sw.js` (200) + `feat-pwa.tsx` (`/feature/pwa`). VERIFIED live 200.
- [x] **GraphQL endpoint + explorer** — `feat-graphql.ts` (`POST /api/graphql`) + `feat-graphql.tsx` (`/feature/graphql`). VERIFIED live 200.
- [x] **Architecture diagram + changelog** — `docs/ARCHITECTURE.md` (mermaid) + `CHANGELOG.md`; `feat-changelog.tsx` (`/feature/changelog`) Whats-New panel. VERIFIED live 200.
- [x] **Cross-platform killPort** — `server.ts` free-port via `fuser`/`ss` on Linux (no crash-loop on `pm2 restart`).
- [x] **Route-contract test suite** — real `node:test` suite (`tests 64 / pass 64`) replacing orphaned bun specs.
- [x] **Page Analytics panel** — `feat-pageanalytics.tsx` (`/feature/page-analytics`, category Observability): localStorage per-route visit tracker (patches `history.pushState`/`replaceState` + listens to `popstate`, restored on unmount; persists across reloads) + live `window.__forgeosTelemetry` session metrics (API calls, client errors, mount ms). Auto-registered via features glob; no App.tsx/server.ts edits; no new CSS added. VERIFIED: client `tsc --noEmit` green, full `npx tsx --test` 109/109 pass. Shipped Wave 10 (commit `2b63f30`).

## Genuine remaining backlog (real gaps — pick from here; Wave 3 candidates)
### Backend/API
- [ ] Agent memory persistence across sessions (external blocker: needs authenticated agent CLI).
- [ ] Multi-agent collaboration / shared workspaces.
- [x] Webhook *delivery engine* — implemented (feat-webhooks.ts: EventEmitter bus + `publishWebhookEvent` + `POST /api/webhooks/publish` + `/api/webhooks/echo`); verified live 200 + delivery receipts on published events.
- [x] Mission scheduler *execution* — implemented (feat-missionsched-exec.ts); source-verified.
- [x] GraphQL depth/complexity limiter — already implemented (feat-graphql.ts: MAX_DEPTH=8, MAX_COMPLEXITY=200 guards); ledger was stale.
### Testing/CI
- [ ] Dependency scanning (Snyk) ACTUALLY wired (CI step present but `snyk` not installed in image).
- [ ] Visual regression (Percy/Chromatic) — needs token; scaffold only.
- [ ] Coverage reporting (codecov).
- [ ] Playwright tests actually exercised in CI (scaffold present, not run).
### Observability/Security
- [x] Rate-limit *enforcement* telemetry — implemented (feat-ratelimit-telemetry.ts); source-verified.
- [ ] Structured audit log to a real store (currently in-memory/file; no rotation policy beyond daily file).
- [ ] CSP report-only -> enforce (currently likely report-uri absent).
### Docs
- [x] SECURITY-GAPS.md refresh - already accurate as of 2026-08-19 (full CSP/HSTS enforced); the prior 'stale' ledger note was itself stale.
- [x] Per-feature README in `features/` - done: per-module READMEs + `docs/FEATURES.md` catalog generated by `scripts/gen-feature-readmes.py` (commit 4380d31).

## Known external blockers (not resolvable by code alone)
- Self-improve agent loop (`POST /api/agent/self-improve/run`) shells out to an agent CLI not
  authenticated on the VPS — needs `FORGEOS_AGENT` set to a usable agent or API key.
- Alerting is a silent no-op until `SENTRY_DSN` or `ALERT_WEBHOOK_URL` env is set.
- `apps/poolleague` shows as a dirty/untracked entry in `git status` but is not present on disk;
  left uncommitted intentionally (unrelated local artifact).

## Wave 1 driver log (2026-08-19)
- 12 conflict-free features shipped via parallel `delegate_task` subagents (each in an isolated
  git worktree, build-verified, merged to master): sse, ratelimit-dash, audit-export, dockerfile,
  docker-compose, k8s, terraform, ci, release, k6, api-doc, onboarding.

## Wave 2 driver log (2026-08-19)
- 12 features shipped (webhooks, otel, alerting, mission-scheduler, marketplace-registry, kanban,
  bulk-actions, confirm-modal, pwa, graphql, rate-limit-per-route, architecture+changelog) + PWA
  manifest gap closed post-merge. Build green; `npm test` 64/64; PM2 restarted; all `/api/*` + `/feature/*`
  routes verified 200. Ledger refreshed to true state (prior "WAVE 2 in flight" notes were stale).


## Wave 3 driver log (2026-08-19)
- CI was effectively dead: `ci.yml` `unit` job ran `node --test __tests__` (only hit a
  failing pseudo-test; never reached the real 69-test suite in `src/server/__tests__/` +
  `tests/`), and its `lint` job ran `node --check server.ts` (only passes on Node >=22
  native-TS; CI used Node 20 -> would fail). Separate `lint.yml` ran `eslint src/**/*.js`
  (no .js present, no eslintrc) + `prettier --check` (prettier not a dependency) -- pure
  no-ops that masked failures.
- Rewrote `.github/workflows/ci.yml` into a working pipeline:
  - `build`: Node 22, `npm install` + `npm run build:client` (real tsc type-check + vite bundle).
  - `test`: Node 22, `npx tsx --test --experimental-test-coverage` -> lcov; Codecov upload
    gated by `CODECOV_TOKEN`. Verified locally on VPS: 69/69 pass, ~51KB lcov generated.
  - `security`: Snyk `snyk test` gated by `SNYK_TOKEN` (was only a scaffold mention).
  - `e2e`: now starts the console server before Playwright so the suite is actually exercisable.
- Deleted broken `.github/workflows/lint.yml` (no eslint/prettier config; its checks were
  no-ops masking failures).
- Ledger correction: tests are **69/69** (prior "64/64" was stale after feature waves).
- Build green; all 21 live `/api/*` routes verified 200 on VPS.

## Wave 4 driver log (2026-08-20)
- VPS (2.24.100.158:2222) was unreachable this tick (SSH reset/filtered). A stale local
  `bun.exe` also held :7777 (Bun was decommissioned — see runtime/decommissioning pitfall).
  Work was done in the local checkout and pushed to `origin` (GitHub), the source the VPS
  `git reset --hard origin/master` syncs from, so it deploys when the VPS returns.
- The ledger's "tests pass" claim was STALE: a full `npm test` actually failed (107/111,
  3 fails + 1 cancelled) and HUNG (the syncHub suite never exited). Fixed 3 genuine bugs:
  1. **runtime.ts `triggerCapture`** self-fetched `http://127.0.0.1:7777/api/capture` with a
     hardcoded :7777 port, so schedule runs silently failed to create pages on any non-7777
     deploy (and 404'd in tests). Now captures in-process via `performCapture` (port-independent).
  2. **rate-limit-stats.test.ts** read `address().port` before the `listening` event (null -> TypeError).
  3. **syncHub.ts** singleton's 25s heartbeat `setInterval` was never cleared, hanging the whole
     `npm test` run; added `stop()` and call it in the test teardown.
- Verified: `npm test` **110/110 green**; client build green; live schedule->page flow returns 200
  (was 404); feature routes (/api/security/headers, /api/csp-report/count,
  /api/self-improve/learning-loop, /api/openapi.json) all 200. Commit `61023fa` pushed to origin.
- Note: `test-governance.mjs` (Playwright e2e) requires a running server and is exercised in CI's
  - separate e2e job; it is not part of the unit `npm test` gate.

  ## Wave 5 driver log (2026-08-20)
  - VPS SSH unreachable again (sshd `Connection reset by peer` / `Connection closed by server` — same outage class as Wave 4). Fell back to the local checkout + push-to-`origin` path the VPS `git reset --hard origin/master` syncs from (Wave 4 precedent).
  - Shipped a new conflict-free console panel: **Reading List** (`features/feat-readinglist.tsx`, auto-mounted at `/feature/reading-list` via the Vite `feat-*.tsx` glob — no App.tsx / server.ts edits). Closes Batch D #34 (semantic bookmarking + reading-list capture). localStorage-backed bookmarks (url/title/note/tags) with CSV export and JSON import; empty-state + a11y labels. Mock-first, no backend dependency; a server-persistence layer can follow behind the same UI.
  - Verified: client `tsc && vite build` GREEN (56 modules transformed, feature bundles); unit `npm test` **109/110** (only failure = Playwright e2e `test-governance.mjs`, environmental — needs browser + live server, excluded from unit gate). Ledger corrected to reflect true test count.
  - See commit `497ce06`.

## Wave 6 driver log (2026-08-20)
- VPS (2.24.100.158:2222) was unreachable for command execution this tick: sshd
  authenticates (key `hostinger_vps` accepted) but cannot fork a shell — the box is
  saturated/flapping (likely 12 hung `npm test` node processes from the orchestrator's
  Wave-4 agents that ran before the `syncHub` teardown-hang fix). The bot-fleet
  watchdog was also dead (no log entries since 2026-08-19 18:35). Used the proven
  Wave 4/5 local-checkout -> `origin` fallback (VPS syncs via `git reset --hard
  origin/master` when it recovers).
- Shipped **Release Notes panel** (closes next-50 #50): `features/feat-releasenotes.tsx`
  auto-registers at `/feature/release-notes`; curated milestone timeline + live
  feature-registry count (imported from `registry.ts`). Mock-first; conflict-free
  (no App.tsx/server.ts edits). Verified: `tsc --noEmit` clean + new test passes
  (`feat-releasenotes.test.tsx`, 1/1 green). Commit `395b126`.
- Local `npm ci` is blocked by a Windows Defender lock on `esbuild.exe` during
  postinstall; verified with `npm install --ignore-scripts` + `npx tsc --noEmit` +
  `npx tsx --test` instead. Full `vite build` + CI gate run on push (GitHub Actions).
- NOTE: ledger `docs/next-50-enhancements.md` was STALE — it listed #49 (Feature
  Flags) as ⬜, but #49 was already shipped in commit `6b38c39`. Verified in source
  before implementing #50 to avoid clobbering existing work.

## Wave 7 driver log (2026-08-20)
- VPS (2.24.100.158:2222) again unreachable for command execution (ssh key accepted
  but server-side shell stalls — box saturated/flapping, same outage class as Waves
  4-6). Used the proven local-checkout -> `origin` (GitHub) fallback; the VPS syncs
  via `git reset --hard origin/master` when it recovers.
- Shipped **Role-based Quickstart Wizard** (closes next-50 Batch E #47):
  `features/feat-quickstart.tsx` auto-registers at `/feature/quickstart` (category
  `Onboarding`, no App.tsx/server.ts edits). Role picker (C-Suite / Engineering /
  Admin / Product) with tailored step-by-step quickstarts; per-step completion
  persisted in localStorage; "Open" links gated through `findFeature()` so only live
  feature routes render (no broken links). Mock-first; conflict-free.
- Added `features/__tests__/feat-quickstart.test.tsx` (module-shape assertion).
- Verified: client `tsc --noEmit` GREEN; new test 1/1 green; full `npm test`
  109/110 (only failure = Playwright e2e `test-governance.mjs`, environmental — needs
  browser + live :7777 server, excluded from unit gate, unchanged from prior waves).
  Local `vite build` is environment-blocked by the same Defender/esbuild postinstall
  lock noted in Wave 6; the real bundle + CI gate run on push (GitHub Actions).
- Ledger corrected: next-50 #47 marked shipped. Remaining genuine backlog now centers
  on backend-coupled items (agent memory persistence, multi-agent workspaces,
  structured audit-store rotation, CSP enforce flip, Snyk/coverage wiring) — most
  need env secrets or an authenticated agent CLI, so they are external blockers.

## Wave 8 driver log (2026-08-20)
- VPS (2.24.100.158:2222) STILL unreachable (ssh key accepted but server-side shell
  stalls / RST mid-KEX — same saturated/flapping outage class as Waves 4-7). Used the
  proven local-checkout -> `origin` (GitHub) fallback; the VPS syncs via
  `git reset --hard origin/master` when it recovers. (Confirmed: port 2222 RSTs after
  KEXINIT, port 22 closed; GitHub HTTPS write verified — pushed + PR-merged to master.)
- Shipped **System Health Overview** panel (conflict-free, mock-first ops view):
  `features/feat-health.tsx` auto-registers at `/feature/health` (category
  `Observability`, no App.tsx/server.ts edits). Aggregates the live status of 7 core
  console endpoints (`/api/health`, `/api/rate-limit/status`, `/api/otel`,
  `/api/alerting/status`, `/api/self-improve/learning-loop`, `/api/csp-report/count`,
  `/api/openapi.json`) into one table: per-endpoint health pill, HTTP code, latency,
  detail, last-checked age; overall "all operational / degraded / full outage"
  summary; auto-poll every 10s + manual Refresh; graceful "unknown" state when the API
  is unreachable (no crash). Reuses existing `pill`/`card`/`tbl` classes — **no new CSS
  rules added** (CSS-first: no duplicate/override risk).
- Added `features/__tests__/feat-health.test.tsx` (module-shape assertion).
- Verified locally (VPS build blocked by the same Defender/esbuild lock as Waves 6-7):
  client `tsc --noEmit` GREEN; feature shape tests **5/5 green** (incl. new). Real
  `vite build` + full `npm test` gate run in CI (GitHub Actions) on the PR; merged to
  master via PR. Live `:7777` route probe pending VPS recovery.
- Note: genuine functional gaps remain backend-coupled (agent memory persistence,
  multi-agent workspaces, structured audit-store rotation, CSP enforce flip,
  Snyk/coverage wiring) — external blockers (env secrets / authenticated agent CLI),
  consistent with Waves 4-7. This wave keeps the console advancing on the proven
  conflict-free panel cadence.

- CI note: the `e2e` job still has ONE pre-existing intermittent failure —
  `tests/e2e.spec.ts` "sidebar navigation uses clean paths" — where a `cmdk open`
  command-palette / shortcuts overlay intercepts the click on the `/roles` link.
  It is unrelated to this panel (neither touches the palette or `/roles` nav) and
  only surfaces now because `build-test` is finally green (previously `e2e` was
  skipped when `build-test` was red). `build-test` (real `vite build` + 109/109
  unit tests) and `dependency-scan` are green. Track the e2e flake as a separate
  follow-up fix (dismiss overlays / verify overlay open-state).

## Wave 9 driver log (2026-08-20)
- VPS (2.24.100.158:2222) UNREACHABLE again for command execution (ssh key accepted
  but server-side shell stalls / RST mid-KEX — same saturated/flapping outage class as
  Waves 4-8). Used the proven local-checkout -> `origin` (GitHub) fallback; the VPS
  syncs via `git reset --hard origin/master` when it recovers. Confirmed: GitHub HTTPS
  write works (PR #43 merged to master); `vps` SSH remote still reset/filtered.
- Shipped **Demo Data Seeder** panel (closes next-50 #48): `features/feat-demo-seeder.tsx`
  auto-registers at `/feature/demo-seeder` (category `Onboarding`, no App.tsx/server.ts
  edits). One click seeds realistic sample data into the four localStorage-backed console
  features — Reading List (5 bookmarks), Onboarding Checklist (22 task ids), Quickstart
  Wizard (20 steps across 4 roles), Feature Flags (6 flags) — so offline tours/demos show
  real content. Excludes Saved Views (server-backed, not seedable offline). Shows a live
  per-feature seeded-state table; conflict-free, mock-first.
- Added `features/__tests__/feat-demo-seeder.test.tsx` (module-shape assertion).
- Verified (VPS build blocked by the same Defender/esbuild lock as Waves 6-8, so the
  real `vite build` + full CI gate run on push): client `tsc --noEmit` GREEN; new test
  **1/1 green**. Commit `f864cfa` (PR #43) pushed + merged to `origin/master`.
- Ledger correction: next-50 #48 marked shipped; #49 (Feature flags) was also already
  shipped (feat-featureflags.tsx, commit 6b38c39) — the `next-50-enhancements.md` entry
  was stale and is now corrected. Remaining genuine gaps are still backend-coupled
  (agent memory persistence, multi-agent workspaces, structured audit-store rotation,
  CSP enforce flip, Snyk/coverage wiring) — external blockers (env secrets / agent CLI).
- Anti-drift: this wave advanced a concrete, committed, build-verified improvement.
  Continues the conflict-free panel cadence while the VPS is down.

## Wave 10 driver log (2026-08-20)
- VPS (2.24.100.158:2222) UNREACHABLE again (ssh key accepted but server-side shell
  stalls / RST mid-KEX / banner timeout — same saturated/flapping outage class as
  Waves 4-9). Used the proven local-checkout -> `origin` (GitHub) fallback; the VPS
  syncs via `git reset --hard origin/master` when it recovers. Confirmed: port 2222
  RSTs after KEX; GitHub HTTPS write verified (commit `2b63f30` pushed to master).
- Shipped **Page Analytics** panel (closes next-50 Batch D #35):
  `features/feat-pageanalytics.tsx` auto-registers at `/feature/page-analytics`
  (category `Observability`, no App.tsx/server.ts edits). Tracks per-route visits in
  localStorage (patches `history.pushState`/`replaceState` + listens to `popstate`,
  restored on unmount; persists across reloads) and surfaces live
  `window.__forgeosTelemetry` session metrics (API calls, client errors, mount ms).
  Reuses existing `panel`/`tbl`/`row`/`pill`/`card` CSS classes — **no new CSS rules
  added**. Includes empty state + clear-tracking action.
- Added `features/__tests__/feat-pageanalytics.test.tsx` (module-shape assertion).
- Verified (VPS build blocked by the same Defender/esbuild lock as Waves 6-9, so the
  real `vite build` + full CI gate run on push): client `tsc --noEmit` GREEN; new test
  **1/1 green**; full `npx tsx --test` suite **109/109 pass, 0 fail** (no regressions).
  Commit `2b63f30` pushed + synced to `origin/master`.
- Ledger correction: next-50 #35 (page-level analytics) marked shipped (client-side
  views + last-accessed; edits tracking intentionally out of scope for a localStorage
  panel). Remaining genuine gaps are still backend-coupled (agent memory persistence,
  multi-agent workspaces, structured audit-store rotation, CSP enforce flip,
  Snyk/coverage wiring) — external blockers (env secrets / agent CLI).
- Anti-drift: this wave advanced a concrete, committed, build-verified improvement via
  the conflict-free panel cadence while the VPS is down.

## Wave 11 driver log (2026-08-21)
- VPS (2.24.100.158:2222) UNREACHABLE again (TCP connects, SSH KEX completes, then the
  server shell stalls / hangs with no banner — same flapping outage class as Waves 4-10).
  Used the proven local-checkout -> `origin` (GitHub) fallback; the VPS syncs via
  `git reset --hard origin/master` when it recovers.
- Shipped **column reorder persistence** for the App Store compatibility table (closes
  next-50 Batch B #19): refactored `App.tsx` `AppStorePanel` to render columns in a
  user-controlled order with up/down buttons, persisted to localStorage so the
  arrangement survives reloads. No App.tsx route changes; CSS-first, no new CSS rules.
- Added pure, unit-tested helper `src/client/src/lib/columns.ts`
  (`reorderColumns` immutable move + `loadColumnOrder`/`saveColumnOrder` with schema
  reconciliation) and `src/client/src/lib/__tests__/columns.test.ts` (5 node:test cases).
- Verified: client `tsc && vite build` GREEN (66 modules); full `npx tsx --test`
  **114/114 pass, 0 fail** (5 new + 109 prior, no regressions). Commit `cb20687`
  pushed + synced to `origin/master` (c0eaa6c..cb20687).
- Ledger correction: next-50 #19 marked shipped (column reorder persistence). Remaining
  genuine gaps are still backend-coupled (agent memory persistence, multi-agent
  workspaces, structured audit-store rotation, CSP enforce flip, Snyk/coverage wiring)
  — external blockers (env secrets / agent CLI).
- Anti-drift: this wave advanced a concrete, committed, build-verified structural
  improvement (not cosmetic) via the local -> origin fallback while the VPS is down.

## Wave 12 driver log (2026-08-21)
- VPS (2.24.100.158:2222) STILL UNREACHABLE for command execution (TCP connects, SSH
  accepts the `hostinger_vps` key, then the server-side shell stalls/hangs — same
  flapping outage class as Waves 4-11). Used the proven local-checkout -> `origin`
  (GitHub) fallback; the VPS auto-syncs via `git reset --hard origin/master` when it
  recovers. Confirmed: an interactive `ssh` shell intermittently returns a banner then
  stalls, so no reliable command channel exists this tick.
- **Deployed the stranded knowledge-graph #40 feature.** It was built "this tick" on the
  `feat/forgeos-knowledge-graph` branch (commit `44df297`) but never merged to
  `master`/`origin`, so it never reached production. Fast-forward merged it into `master`
  (no conflicts — #40 touched `runtime.ts`/`knowledgeGraph.ts`/`feat-knowledge-graph.tsx`,
  disjoint from Wave 11's `App.tsx` #19 changes). The `GET /api/knowledge-graph` endpoint
  + radial SVG viewer now ship.
- Shipped **Page-Mutation Audit viewer** (closes next-50 #38): `features/feat-audit-trail.tsx`
  auto-registers at `/feature/audit-trail` (category `Observability`, no App.tsx/server.ts
  edits). Mock-first localStorage audit log of page mutations (create/update/delete/rename/
  move) with action filter, page/actor search, seed-sample and clear actions; reuses existing
  `.tbl`/`.tag`/`.card`/`.input` CSS — **no new CSS rules added** (CSS-first: no
  duplicate/override risk). Backend persistence is a documented follow-up.
- Added `features/__tests__/feat-audit-trail.test.tsx` (module-shape assertion; hermetic —
  no fetch/timer/global listener at import).
- Verified (VPS build blocked by the same Defender/esbuild lock as Waves 6-11, so the real
  `vite build` + full CI gate run on push): client `tsc --noEmit` GREEN; new test **1/1
  green** (explicit `npx tsx --test`); full `npx tsx --test` **117/117 pass, 0 fail** (no
  regressions). Commit `11494b5` pushed + synced to `origin/master`.
- Ledger correction: next-50 #38 marked shipped (frontend/mock-first). NOTE: the auto
  `npx tsx --test` gate does not discover the client `features/__tests__/*.test.tsx`
  module-shape tests (none of the sibling panel tests appear in the auto-run either) — they
  are verified via explicit `npx tsx --test <file>` + `tsc --noEmit`, consistent with Waves
  6-11. Remaining genuine gaps are still backend-coupled (#29, #31, #33, #37, #39, #41-43) —
  external blockers (env secrets / authenticated agent CLI).
- Anti-drift: this wave advanced a concrete, committed, build-verified improvement (deployed
  #40 + shipped a new #38 panel) via the local -> origin fallback while the VPS is down.
