{
  "name": "forgeos-brain-console",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build ./src/app.ts --outfile ./public/app.js --format esm --target browser --sourcemap=none",
    "start": "bun run server.ts",
    "dev": "bun run build && bun run server.ts"
  },
  "description": "ForgeOS Brain Console — web UI over the isolated gbrain (C:\\ForgeOS)."
}
