# ForgeOS Brain Console — Enhancement Backlog

## Sprint 1: Quick Wins (1-2h each)
1. Expand tooltips to ALL form fields, buttons, table headers, status pills, empty states
2. Add render tests for all Phase 11 panels (wizard, projects, settings, workflows, marketplace, plugins)
3. Add integration tests for Phase 6-10 endpoints (auth, state, backup, metrics, webhooks)
4. Keyboard shortcut cheatsheet (press `?` or `cmd+shift+/`)
5. Webhook management UI (CRUD for webhooks from console)
6. Print styles for governance/ledger/capture
7. Empty state illustrations for vault/audit/federation

## Sprint 2: Backend Hardening (2-4h each)
8. Auth middleware tests (JWT validation, expired token, public vs protected)
9. Rate limiter tests (per-route limits, IP bucketing, window expiry)
10. Webhook retry/backoff (exponential backoff, dead-letter queue)
11. Plugin hot-reload (watch pluginsDir, reload without restart)
12. Request/response logging to file (structured JSON)
13. API request deduplication (cache identical in-flight requests)
14. Health check dependencies (Ollama, gbrain, pglite with proper timeouts)

## Sprint 3: UI Polish (2-4h each)
15. Drag-and-drop kanban (move work items between columns)
16. Bulk actions in vault/audit (select multiple rows, batch delete/export)
17. Live search/filter in tables (debounced, client-side)
18. Column visibility toggles for tables
19. Confirmation modals for destructive actions
20. Dark mode by default with system preference detection

## Sprint 4: Developer Experience (2-4h each)
21. Hot reload for SPA (watch src/app.js, inject changes)
22. Linting/formatting CI (ESLint + Prettier in GitHub Actions)
23. Pre-commit hooks (format, lint, test)
24. OpenAPI client generation (TypeScript fetch client)
25. Local dev environment script (just dev or make dev)

## Sprint 5: Observability & Security (2-4h each)
26. Error tracking (Sentry integration for frontend + backend)
27. Structured log aggregation (Loki/ELK stack)
28. Distributed tracing (OpenTelemetry, trace IDs)
29. CSRF protection (SameSite cookies, double-submit token)
30. Content Security Policy headers
31. Dependency scanning in CI (bun audit or Snyk)

## Sprint 6: Advanced (half-day+)
32. GraphQL endpoint
33. Plugin sandboxing (timeouts, memory limits)
34. PWA offline mode (manifest.json, push notifications)
35. Agent memory/context persistence
36. Mission scheduling (cron-like agent dispatch)
37. Model fallback chains (auto-switch on rate limit)
