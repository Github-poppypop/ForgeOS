# feat-template (client)

> Client feature — `src/client/src/features/feat-template.tsx`

**Mounts at:** `/feature/template` · **Label:** Template Feature · **Category:** Features

TEMPLATE FEATURE -- safe to delete. Demonstrates the self-contained feature contract. Copy this file to features/feat-<name>.tsx and edit; it will auto-appear in the sidebar and route dispatch with NO edits to App.tsx or server.ts. Note: this project uses the automatic JSX runtime, so you do NOT import React. Import hooks directly, e.g. `import { useState } from 'react';`.

---

_Auto-generated from source. Edit the module to change behaviour._
